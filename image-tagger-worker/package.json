{
  "name": "image-tagger-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Standalone worker that claims queued image-tagging jobs and tags them via the image tagger API.",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "dotenv": "^17.2.3",
    "kysely": "^0.28.8",
    "minio": "^8.0.6",
    "pg": "^8.16.3",
    "tsx": "^4.21.0"
  },
  "devDependencies": {
    "@types/node": "^22",
    "@types/pg": "^8.15.5",
    "typescript": "^5.9.2"
  }
}
