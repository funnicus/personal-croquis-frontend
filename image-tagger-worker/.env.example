# Optional host-side worker overrides.
# Shared database, MinIO, and tagger API values are loaded from ../.env by default.

# Optional tuning
# SIGNED_URL_EXPIRY_SECONDS=3600
# WORKER_POLL_INTERVAL_MS=2000
# WORKER_BATCH_SIZE=8
